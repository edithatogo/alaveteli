Task: Run regression and security gate (harness repair). Summary: Installed Brakeman explicitly in the security workflow so the scanner runs outside the application bundle, and corrected two request specs to require the repo's existing spec_helper instead of missing rails_helper. Files: .github/workflows/security.yml; spec/requests/contracts_validation_spec.rb; spec/requests/turnstile_challenge_spec.rb. Why: CI showed deterministic harness failures before the real Phase 3 regression and security gates could provide useful feedback.
